  gateways:
  - device: mqtt
    persistence_file: '/home/homeassistant/.homeassistant/.storage/mysensors5.json'
    topic_in_prefix: 'mygateway1-out'
    topic_out_prefix: 'mygateway1-in'
  version: 2.0
  persistence: true
  optimistic: false

  

# mysensors:
#   gateways:
#    - device: mqtt
#      topic_in_prefix: 'mysensors-in'
#      topic_out_prefix: 'mysensors-out'
