  gateways:
  - device: mqtt
    persistence_file: '/config/mysensors/mysensors_devices.json'
    topic_in_prefix: 'mysensors-in'
    topic_out_prefix: 'mysensors-out'
  version: 2.0
  persistence: true
  optimistic: false

  

# mysensors:
#   gateways:
#    - device: mqtt
#      topic_in_prefix: 'mysensors-in'
#      topic_out_prefix: 'mysensors-out'
