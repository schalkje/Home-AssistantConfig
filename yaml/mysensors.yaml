  gateways:
  - device: mqtt
    persistence_file: '/config/mysensors_devices.json'
    tcp_port: 5003
  version: 2.0
  persistence: true
  optimistic: false

  

# mysensors:
#   gateways:
#    - device: mqtt
#      topic_in_prefix: 'mysensors-in'
#      topic_out_prefix: 'mysensors-out'
